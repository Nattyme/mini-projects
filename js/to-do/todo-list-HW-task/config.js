import { BUTTON_TYPES } from './view/UI/buttonTypes.js';
import { RULES } from './model/validate/validateRules.js';

const config = {
  RULES,
  BUTTON_TYPES,
  MIN_LENGTH,
  MAX_LENGTH,
  ALLOWED,
  MIN_VALUE
};

export default config;
